[tool.black]
line-length = 100
target-version = ['py311']

[tool.flake8]
max-line-length = 100
exclude = [
    "venv",
    "__pycache__",
    ".git",
    ".pytest_cache",
    ".specify"
]
ignore = ["E203", "W503"]

[tool.isort]
profile = "black"
line_length = 100
